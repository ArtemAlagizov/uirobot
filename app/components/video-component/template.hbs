{{ivy-videojs
        controls=controls
        fluid=fluid
        loop=loop
        muted=muted
        play=(action "play")
        playing=(action "playing")
        playbackRate=playbackRate
        poster=poster
        ready=(action "ready")
        setup=setup
        src=src
}}